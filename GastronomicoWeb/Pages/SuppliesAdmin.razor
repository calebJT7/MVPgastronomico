@page "/insumos"
@using RotiseriaWeb.Models
@using RotiseriaWeb.Services
@inject HttpClient Http
@inject ISnackbar Snackbar
@inject IDialogService DialogService

<PageTitle>Insumos y Stock - SaaS Gastronómico</PageTitle>

<FeatureGate Feature="@FeatureCodes.AdvancedInventory"
             FeatureName="Control Avanzado de Insumos y Stock"
             Description="Gestioná materias primas e ingredientes (harina, queso, carnes, bebidas), puntos de reposición con alertas de stock mínimo, registro de compras a proveedores y mermas con costeo automático.">

<MudContainer MaxWidth="MaxWidth.Large" Class="py-2 mb-8">
    <div class="d-flex flex-wrap justify-space-between align-center mb-6 gap-2">
        <div>
            <MudText Typo="Typo.h4" Style="font-weight: 700; letter-spacing: -0.02em;">Insumos y Materias Primas</MudText>
            <MudText Typo="Typo.body2" Color="Color.Secondary">Control de existencias, cálculo de CMV, registro de compras a proveedores y reporte de mermas.</MudText>
        </div>
        <div class="d-flex flex-wrap gap-2">
            <MudButton Variant="Variant.Outlined" Color="Color.Warning" StartIcon="@Icons.Material.Filled.RemoveCircleOutline" OnClick="OpenWasteDialog" Size="Size.Small">
                Registrar Merma
            </MudButton>
            <MudButton Variant="Variant.Outlined" Color="Color.Success" StartIcon="@Icons.Material.Filled.AddShoppingCart" OnClick="OpenPurchaseDialog" Size="Size.Small">
                Ingreso de Compra
            </MudButton>
            <MudButton Variant="Variant.Filled" Color="Color.Primary" StartIcon="@Icons.Material.Filled.Add" OnClick="OpenCreateDialog" Size="Size.Small">
                Nuevo Insumo
            </MudButton>
        </div>
    </div>

    <!-- Metrics Cards -->
    <MudGrid Spacing="3" Class="mb-4">
        <MudItem xs="12" sm="4">
            <div class="saas-kpi-card h-100 d-flex flex-column justify-space-between">
                <div>
                    <div class="d-flex justify-space-between align-start mb-2">
                        <span class="saas-kpi-label">Total Insumos</span>
                        <div class="saas-kpi-icon-wrapper">
                            <MudIcon Icon="@Icons.Material.Filled.Warehouse" Size="Size.Small" />
                        </div>
                    </div>
                    <div class="saas-kpi-value">@supplies.Count</div>
                </div>
                <div class="saas-kpi-subtext">Materias primas activas en almacén</div>
            </div>
        </MudItem>

        <MudItem xs="12" sm="4">
            <div class="saas-kpi-card h-100 d-flex flex-column justify-space-between">
                <div>
                    <div class="d-flex justify-space-between align-start mb-2">
                        <span class="saas-kpi-label">Stock Crítico</span>
                        <div class="saas-kpi-icon-wrapper" style="background-color: var(--saas-error-bg); color: var(--saas-error-text);">
                            <MudIcon Icon="@Icons.Material.Filled.WarningAmber" Size="Size.Small" />
                        </div>
                    </div>
                    <div class="saas-kpi-value" style="color: var(--saas-error-text);">
                        @supplies.Count(s => s.IsLowStock)
                    </div>
                </div>
                <div class="saas-kpi-subtext">Insumos por debajo del punto de reposición</div>
            </div>
        </MudItem>

        <MudItem xs="12" sm="4">
            <div class="saas-kpi-card h-100 d-flex flex-column justify-space-between">
                <div>
                    <div class="d-flex justify-space-between align-start mb-2">
                        <span class="saas-kpi-label">Valor en Inventario</span>
                        <div class="saas-kpi-icon-wrapper" style="background-color: var(--saas-success-bg); color: var(--saas-success-text);">
                            <MudIcon Icon="@Icons.Material.Filled.AttachMoney" Size="Size.Small" />
                        </div>
                    </div>
                    <div class="saas-kpi-value">
                        $@supplies.Sum(s => s.CurrentStock * s.CostPerUnit).ToString("N2")
                    </div>
                </div>
                <div class="saas-kpi-subtext">Valoración monetaria del stock actual</div>
            </div>
        </MudItem>
    </MudGrid>

    <!-- Filter Bar -->
    <div class="mud-paper pa-3 mb-4 d-flex flex-wrap align-center justify-space-between gap-3">
        <div class="d-flex align-center gap-3 flex-grow-1" style="max-width: 500px;">
            <MudTextField @bind-Value="searchString" Placeholder="Buscar por nombre o proveedor..." Adornment="Adornment.Start"
                          AdornmentIcon="@Icons.Material.Filled.Search" Variant="Variant.Outlined" Margin="Margin.Dense" Immediate="true" />
            <MudButton Variant="Variant.Filled" Color="Color.Primary" OnClick="LoadSupplies" Size="Size.Small">
                Filtrar
            </MudButton>
        </div>
        <div>
            <MudSwitch @bind-Value="onlyLowStock" Label="Ver solo stock bajo mínimo" Color="Color.Error" />
        </div>
    </div>

    @if (loading)
    {
        <MudProgressLinear Color="Color.Primary" Indeterminate="true" Class="my-4" />
    }
    else if (!filteredSupplies.Any())
    {
        <div class="mud-paper pa-8 text-center text-muted">
            <MudIcon Icon="@Icons.Material.Filled.Inventory2" Size="Size.Large" Color="Color.Secondary" Class="mb-2" />
            <MudText Typo="Typo.subtitle1" Style="font-weight: 600;">No se encontraron insumos</MudText>
            <MudText Typo="Typo.body2" Color="Color.Secondary">Podés dar de alta materias primas desde el botón superior.</MudText>
        </div>
    }
    else
    {
        <div class="mud-paper overflow-hidden">
            <MudTable Items="@filteredSupplies" Dense="true" Hover="true" Elevation="0">
                <HeaderContent>
                    <MudTh>Insumo</MudTh>
                    <MudTh>Unidad</MudTh>
                    <MudTh Style="text-align: right;">Stock Actual</MudTh>
                    <MudTh Style="text-align: right;">Stock Mínimo</MudTh>
                    <MudTh Style="text-align: right;">Costo Unit.</MudTh>
                    <MudTh Style="text-align: right;">Valor Total</MudTh>
                    <MudTh>Proveedor</MudTh>
                    <MudTh>Estado</MudTh>
                    <MudTh Style="text-align: right;">Acciones</MudTh>
                </HeaderContent>
                <RowTemplate>
                    <MudTd>
                        <span style="font-weight: 600; color: var(--saas-text-primary);">@context.Name</span>
                    </MudTd>
                    <MudTd>
                        <span class="saas-badge saas-badge-neutral">@context.Unit</span>
                    </MudTd>
                    <MudTd Style="text-align: right; font-weight: 700; color: var(--saas-text-primary);">
                        @context.CurrentStock.ToString("N2")
                    </MudTd>
                    <MudTd Style="text-align: right; color: var(--saas-text-secondary);">
                        @context.MinimumStock.ToString("N2")
                    </MudTd>
                    <MudTd Style="text-align: right;">
                        $@context.CostPerUnit.ToString("N2")
                    </MudTd>
                    <MudTd Style="text-align: right; font-weight: 600;">
                        $@((context.CurrentStock * context.CostPerUnit).ToString("N2"))
                    </MudTd>
                    <MudTd>@(string.IsNullOrWhiteSpace(context.Supplier) ? "-" : context.Supplier)</MudTd>
                    <MudTd>
                        @if (context.IsLowStock)
                        {
                            <span class="saas-badge saas-badge-error">Reponer Stock</span>
                        }
                        else
                        {
                            <span class="saas-badge saas-badge-success">Óptimo</span>
                        }
                    </MudTd>
                    <MudTd Style="text-align: right;">
                        <MudTooltip Text="Editar Insumo">
                            <MudIconButton Icon="@Icons.Material.Filled.Edit" Size="Size.Small" Color="Color.Default"
                                           OnClick="@(() => OpenEditDialog(context))" Style="width: 28px; height: 28px; padding: 2px;" />
                        </MudTooltip>
                        <MudTooltip Text="Eliminar Insumo">
                            <MudIconButton Icon="@Icons.Material.Filled.DeleteOutline" Size="Size.Small" Color="Color.Error"
                                           OnClick="@(() => DeleteSupply(context.Id))" Style="width: 28px; height: 28px; padding: 2px;" />
                        </MudTooltip>
                    </MudTd>
                </RowTemplate>
            </MudTable>
        </div>
    }

    <!-- Dialog Create / Edit Supply -->
    <MudDialog @bind-Visible="supplyDialogVisible" Options="dialogOptions">
        <TitleContent>
            <div class="d-flex align-center gap-2">
                <MudIcon Icon="@Icons.Material.Filled.Inventory" Size="Size.Small" />
                <MudText Typo="Typo.subtitle1" Style="font-weight: 700;">@(isEditing ? "Editar Insumo" : "Nuevo Insumo")</MudText>
            </div>
        </TitleContent>
        <DialogContent>
            <MudTextField @bind-Value="supplyModel.Name" Label="Nombre del Insumo (ej. Harina 000, Muzzarella)" Variant="Variant.Outlined" Required="true" Class="mb-3" />
            <MudSelect @bind-Value="supplyModel.Unit" Label="Unidad de Medida" Variant="Variant.Outlined" Class="mb-3" AnchorOrigin="Origin.BottomCenter">
                <MudSelectItem Value="@("kg")">Kilogramos (kg)</MudSelectItem>
                <MudSelectItem Value="@("gr")">Gramos (gr)</MudSelectItem>
                <MudSelectItem Value="@("lt")">Litros (lt)</MudSelectItem>
                <MudSelectItem Value="@("ml")">Mililitros (ml)</MudSelectItem>
                <MudSelectItem Value="@("un")">Unidades (un)</MudSelectItem>
                <MudSelectItem Value="@("porcion")">Porción</MudSelectItem>
            </MudSelect>
            <div class="d-flex gap-3 mb-3">
                <MudNumericField @bind-Value="supplyModel.CurrentStock" Label="Stock Inicial" Variant="Variant.Outlined" Min="0m" Format="N2" />
                <MudNumericField @bind-Value="supplyModel.MinimumStock" Label="Stock Mínimo Alerta" Variant="Variant.Outlined" Min="0m" Format="N2" />
            </div>
            <div class="d-flex gap-3 mb-3">
                <MudNumericField @bind-Value="supplyModel.CostPerUnit" Label="Costo por Unidad ($)" Variant="Variant.Outlined" Min="0m" Format="N2" />
                <MudTextField @bind-Value="supplyModel.Supplier" Label="Proveedor Habitual (Opcional)" Variant="Variant.Outlined" />
            </div>
        </DialogContent>
        <DialogActions>
            <MudButton Variant="Variant.Outlined" OnClick="@(() => supplyDialogVisible = false)">Cancelar</MudButton>
            <MudButton Variant="Variant.Filled" Color="Color.Primary" OnClick="SaveSupplyAsync" Disabled="saving">
                @(saving ? "Guardando..." : "Guardar Insumo")
            </MudButton>
        </DialogActions>
    </MudDialog>

    <!-- Dialog Register Waste / Merma -->
    <MudDialog @bind-Visible="wasteDialogVisible" Options="dialogOptions">
        <TitleContent>
            <div class="d-flex align-center gap-2">
                <MudIcon Icon="@Icons.Material.Filled.RemoveCircleOutline" Color="Color.Warning" Size="Size.Small" />
                <MudText Typo="Typo.subtitle1" Style="font-weight: 700;">Registrar Desperdicio o Merma</MudText>
            </div>
        </TitleContent>
        <DialogContent>
            <MudSelect @bind-Value="wasteModel.SupplyId" Label="Seleccionar Insumo" Variant="Variant.Outlined" Class="mb-3" AnchorOrigin="Origin.BottomCenter">
                @foreach (var s in supplies)
                {
                    <MudSelectItem Value="@s.Id">@s.Name (@s.Unit) - Actual: @s.CurrentStock @s.Unit</MudSelectItem>
                }
            </MudSelect>
            <MudNumericField @bind-Value="wasteModel.Quantity" Label="Cantidad de Merma" Variant="Variant.Outlined" Min="0.01m" Format="N2" Class="mb-3" />
            <MudTextField @bind-Value="wasteModel.Reason" Label="Causa de la Merma (vencimiento, cocción, rotura)" Variant="Variant.Outlined" Lines="2" Class="mb-3" />
        </DialogContent>
        <DialogActions>
            <MudButton Variant="Variant.Outlined" OnClick="@(() => wasteDialogVisible = false)">Cancelar</MudButton>
            <MudButton Variant="Variant.Filled" Color="Color.Warning" OnClick="SaveWasteAsync" Disabled="savingWaste">
                @(savingWaste ? "Registrando..." : "Confirmar Merma")
            </MudButton>
        </DialogActions>
    </MudDialog>

    <!-- Dialog Quick Purchase Entry -->
    <MudDialog @bind-Visible="purchaseDialogVisible" Options="dialogOptions">
        <TitleContent>
            <div class="d-flex align-center gap-2">
                <MudIcon Icon="@Icons.Material.Filled.AddShoppingCart" Color="Color.Success" Size="Size.Small" />
                <MudText Typo="Typo.subtitle1" Style="font-weight: 700;">Ingreso de Compra a Proveedor</MudText>
            </div>
        </TitleContent>
        <DialogContent>
            <MudTextField @bind-Value="purchaseSupplierName" Label="Proveedor" Variant="Variant.Outlined" Class="mb-3" />
            <MudTextField @bind-Value="purchaseInvoiceNumber" Label="N° Factura / Remito (Opcional)" Variant="Variant.Outlined" Class="mb-3" />

            <MudSelect @bind-Value="purchaseSupplyId" Label="Insumo Adquirido" Variant="Variant.Outlined" Class="mb-3" AnchorOrigin="Origin.BottomCenter">
                @foreach (var s in supplies)
                {
                    <MudSelectItem Value="@s.Id">@s.Name (@s.Unit)</MudSelectItem>
                }
            </MudSelect>
            <div class="d-flex gap-3 mb-3">
                <MudNumericField @bind-Value="purchaseQuantity" Label="Cantidad Comprada" Variant="Variant.Outlined" Min="0.01m" Format="N2" />
                <MudNumericField @bind-Value="purchaseUnitCost" Label="Precio Unitario ($)" Variant="Variant.Outlined" Min="0m" Format="N2" />
            </div>
            @if (purchaseQuantity > 0 && purchaseUnitCost > 0)
            {
                <div class="pa-3 rounded mb-2" style="background-color: var(--saas-success-bg); border: 1px solid var(--saas-success-border); color: var(--saas-success-text); font-size: 0.875rem;">
                    Total Compra a Ingresar: <b>$@((purchaseQuantity * purchaseUnitCost).ToString("N2"))</b>
                </div>
            }
        </DialogContent>
        <DialogActions>
            <MudButton Variant="Variant.Outlined" OnClick="@(() => purchaseDialogVisible = false)">Cancelar</MudButton>
            <MudButton Variant="Variant.Filled" Color="Color.Success" OnClick="SavePurchaseAsync" Disabled="savingPurchase">
                @(savingPurchase ? "Registrando..." : "Confirmar Compra y Sumar Stock")
            </MudButton>
        </DialogActions>
    </MudDialog>
</MudContainer>

</FeatureGate>

@code {
    private List<SupplyDto> supplies = new();
    private bool loading = true;
    private string searchString = string.Empty;
    private bool onlyLowStock = false;

    private bool supplyDialogVisible = false;
    private bool isEditing = false;
    private int editingSupplyId = 0;
    private SupplyWriteRequest supplyModel = new();
    private bool saving = false;

    private bool wasteDialogVisible = false;
    private RegisterWasteRequest wasteModel = new();
    private bool savingWaste = false;

    private bool purchaseDialogVisible = false;
    private string purchaseSupplierName = string.Empty;
    private string purchaseInvoiceNumber = string.Empty;
    private int purchaseSupplyId = 0;
    private decimal purchaseQuantity = 0;
    private decimal purchaseUnitCost = 0;
    private bool savingPurchase = false;

    private DialogOptions dialogOptions = new() { MaxWidth = MaxWidth.Small, FullWidth = true, CloseButton = true };

    private IEnumerable<SupplyDto> filteredSupplies => supplies.Where(s =>
        (string.IsNullOrWhiteSpace(searchString) || s.Name.Contains(searchString, StringComparison.OrdinalIgnoreCase) || (s.Supplier ?? "").Contains(searchString, StringComparison.OrdinalIgnoreCase)) &&
        (!onlyLowStock || s.IsLowStock));

    protected override async Task OnInitializedAsync()
    {
        await LoadSupplies();
    }

    private async Task LoadSupplies()
    {
        loading = true;
        try
        {
            var url = $"api/supplies?q={Uri.EscapeDataString(searchString)}&onlyLowStock={onlyLowStock.ToString().ToLower()}";
            var res = await Http.GetFromJsonAsync<List<SupplyDto>>(url);
            supplies = res ?? new();
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Error al cargar insumos: {ex.Message}", Severity.Error);
        }
        finally
        {
            loading = false;
        }
    }

    private void OpenCreateDialog()
    {
        isEditing = false;
        editingSupplyId = 0;
        supplyModel = new SupplyWriteRequest { Unit = "kg" };
        supplyDialogVisible = true;
    }

    private void OpenEditDialog(SupplyDto dto)
    {
        isEditing = true;
        editingSupplyId = dto.Id;
        supplyModel = new SupplyWriteRequest
        {
            Name = dto.Name,
            Unit = dto.Unit,
            CurrentStock = dto.CurrentStock,
            MinimumStock = dto.MinimumStock,
            CostPerUnit = dto.CostPerUnit,
            Supplier = dto.Supplier
        };
        supplyDialogVisible = true;
    }

    private async Task SaveSupplyAsync()
    {
        if (string.IsNullOrWhiteSpace(supplyModel.Name))
        {
            Snackbar.Add("El nombre del insumo es requerido", Severity.Warning);
            return;
        }

        saving = true;
        try
        {
            HttpResponseMessage res;
            if (isEditing)
            {
                res = await Http.PutAsJsonAsync($"api/supplies/{editingSupplyId}", supplyModel);
            }
            else
            {
                res = await Http.PostAsJsonAsync("api/supplies", supplyModel);
            }

            if (res.IsSuccessStatusCode)
            {
                Snackbar.Add("Insumo guardado correctamente", Severity.Success);
                supplyDialogVisible = false;
                await LoadSupplies();
            }
            else
            {
                var err = await res.Content.ReadAsStringAsync();
                Snackbar.Add($"Error al guardar: {err}", Severity.Error);
            }
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Error: {ex.Message}", Severity.Error);
        }
        finally
        {
            saving = false;
        }
    }

    private async Task DeleteSupply(int id)
    {
        try
        {
            var res = await Http.DeleteAsync($"api/supplies/{id}");
            if (res.IsSuccessStatusCode)
            {
                Snackbar.Add("Insumo eliminado", Severity.Success);
                await LoadSupplies();
            }
            else
            {
                Snackbar.Add("Error al eliminar insumo", Severity.Error);
            }
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Error: {ex.Message}", Severity.Error);
        }
    }

    private void OpenWasteDialog()
    {
        wasteModel = new RegisterWasteRequest { SupplyId = supplies.FirstOrDefault()?.Id ?? 0, Quantity = 0 };
        wasteDialogVisible = true;
    }

    private async Task SaveWasteAsync()
    {
        if (wasteModel.SupplyId <= 0 || wasteModel.Quantity <= 0)
        {
            Snackbar.Add("Seleccioná un insumo y una cantidad válida", Severity.Warning);
            return;
        }

        savingWaste = true;
        try
        {
            var res = await Http.PostAsJsonAsync("api/supplies/waste", wasteModel);
            if (res.IsSuccessStatusCode)
            {
                Snackbar.Add("Merma registrada y stock descontado", Severity.Success);
                wasteDialogVisible = false;
                await LoadSupplies();
            }
            else
            {
                var err = await res.Content.ReadAsStringAsync();
                Snackbar.Add($"Error: {err}", Severity.Error);
            }
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Error: {ex.Message}", Severity.Error);
        }
        finally
        {
            savingWaste = false;
        }
    }

    private void OpenPurchaseDialog()
    {
        purchaseSupplierName = string.Empty;
        purchaseInvoiceNumber = string.Empty;
        purchaseSupplyId = supplies.FirstOrDefault()?.Id ?? 0;
        purchaseQuantity = 0;
        purchaseUnitCost = supplies.FirstOrDefault()?.CostPerUnit ?? 0;
        purchaseDialogVisible = true;
    }

    private async Task SavePurchaseAsync()
    {
        if (purchaseSupplyId <= 0 || purchaseQuantity <= 0)
        {
            Snackbar.Add("Seleccioná un insumo y una cantidad válida", Severity.Warning);
            return;
        }

        savingPurchase = true;
        try
        {
            var req = new RegisterPurchaseRequest
            {
                SupplierName = string.IsNullOrWhiteSpace(purchaseSupplierName) ? "Proveedor General" : purchaseSupplierName,
                InvoiceNumber = purchaseInvoiceNumber,
                Date = DateTime.UtcNow,
                Items = new List<PurchaseItemInputDto>
                {
                    new PurchaseItemInputDto
                    {
                        SupplyId = purchaseSupplyId,
                        Quantity = purchaseQuantity,
                        UnitCost = purchaseUnitCost
                    }
                }
            };

            var res = await Http.PostAsJsonAsync("api/supplies/purchases", req);
            if (res.IsSuccessStatusCode)
            {
                Snackbar.Add("Compra registrada y stock actualizado", Severity.Success);
                purchaseDialogVisible = false;
                await LoadSupplies();
            }
            else
            {
                var err = await res.Content.ReadAsStringAsync();
                Snackbar.Add($"Error: {err}", Severity.Error);
            }
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Error: {ex.Message}", Severity.Error);
        }
        finally
        {
            savingPurchase = false;
        }
    }
}
